{
  "name": "pool-intelligence-pro",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd frontend && npm ci && cd ../backend && npm ci --include=dev",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npx prisma generate && npm run build",
    "copy:frontend": "cp -r frontend/dist backend/public",
    "build": "npm run build:frontend && npm run copy:frontend && npm run build:backend",
    "start": "cd backend && npm start"
  }
}
